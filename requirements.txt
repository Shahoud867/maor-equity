# Core dependencies — CPU-capable experiments (H3, chunk-filter, all tests).
#
# For the GPU node, install torch from the CUDA index FIRST, then this file:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements-gpu.txt
#
# Version floors reflect what the code requires, not what happened to be
# installed. transformers>=5 removed script-based dataset loading and changed
# tokenizer construction; src/maor/agents/sentiment.py carries a compatibility
# path for legacy checkpoints (yiyanghkust/finbert-tone) that depends on it.

torch>=2.0
transformers>=4.40
tokenizers>=0.15
datasets>=2.18
huggingface-hub>=0.23

numpy>=1.24
scipy>=1.10
scikit-learn>=1.3

rouge-score>=0.1.2
pyyaml>=6.0

# finbert-tone ships only vocab.txt; the tokenizer converter needs this.
sentencepiece>=0.1.99
