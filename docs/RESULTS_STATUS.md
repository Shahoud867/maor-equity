# Results status

Generated by `python -m maor.cli report`. Do not edit by hand.

Last generated: 2026-08-28 14:25 UTC

**2 of 5 claims have a provenance-stamped result file.**

A claim with no result file is PENDING. It is not an estimate, and no number
for it may appear in the paper. See `docs/EVIDENCE_POLICY.md`.

| Claim | Status | Outcome | Measured | Command |
|---|---|---|---|---|
| H1 | PENDING (GPU) | — | — | `python -m maor.cli h1-latency --config configs/gpu_t1000.yaml --ablation full` |
| H2 | PENDING (GPU) | — | — | `python -m maor.cli h2-summarisation --config configs/gpu_t1000.yaml --n-samples 100` |
| H3 | MEASURED | H3a=FAIL, H3b=FAIL | 2026-08-28 @ `c3bb79ac` (dirty) | `python -m maor.cli h3-sentiment` |
| VRAM | PENDING (GPU) | — | — | `python -m maor.cli vram-verify --config configs/gpu_t1000.yaml` |
| CHUNK | MEASURED | - | 2026-08-28 @ `91a88097` (dirty) | `python -m maor.cli chunk-filter` |

## H3 — Multi-dimensional sentiment changes and improves directional recommendations

- Result file: `results/h3_sentiment.json`
- Measured on: 2026-08-28T14:18:00+00:00 (LAPTOP-LLVL1LSJ)
- Hardware: CPU-only, 12 cores
- Commit: `c3bb79ac3c3bdafbd8b4c8c81a4f181995b7620f` **(working tree dirty)**
- Config SHA: `ebfa0d10c043` | seed: 20260428

**H3a: FAIL** — direction_divergence_rate = 1.548 % of texts (threshold > 10.0)

> Descriptive only. Divergence is not itself evidence of quality: a rule that flips answers at random would score highly here. H3b is the decision-relevant test.

**H3b: FAIL** — accuracy_delta_vs_b3 = -0.743 pp (threshold > 0.0)

> Paired bootstrap 95% CI on the accuracy delta: [-1.09, -0.41] pp; paired permutation p=0.0002. A positive point estimate whose CI spans zero is not evidence of improvement.

**Validity threats carried by this result:**

- *(critical)* **train-on-test contamination** — ProsusAI/finbert is fine-tuned on Financial PhraseBank (Araci 2019; see the model card). Evaluating it on PhraseBank measures memorisation of the training set, not generalisation. Absolute accuracy figures from this run are NOT valid estimates of field performance and must not be reported as such.
- *(major)* **regulatory dimension rarely activated** — Only 8/4846 texts (0.2%) matched the regulatory router, so the veto rule affects too few cases to support a claim in either direction.
- *(major)* **sentence-level granularity mismatch** — The deployed pipeline routes 512-token filing chunks; this evaluation routes single sentences. A sentence rarely carries both market and regulatory content, which structurally suppresses the interaction the multi-dimensional rule is designed to capture.

## CHUNK — ChunkFilter cost and document coverage

- Result file: `results/chunk_filter_study.json`
- Measured on: 2026-08-28T11:48:26+00:00 (LAPTOP-LLVL1LSJ)
- Hardware: CPU-only, 12 cores
- Commit: `91a88097a2850f5a6707b3b5f90fe98a8057065c` **(working tree dirty)**
- Config SHA: `ab1cf2ac7cb4` | seed: 20260428

## Not measured, and why

Two different reasons, which should not be conflated.

**Needs a CUDA device.** H1, H2 and VRAM verification are implemented and
unit-tested against injected executors, but unrun: the development machine
has no CUDA device (Intel integrated graphics only). Producing numbers for
them here would mean inventing them, which is the failure this remediation
exists to correct. Follow `docs/GPU_RUNBOOK.md` on a GPU machine, then
re-run `python -m maor.cli report` to regenerate this page.

**Not implemented.** The *distribution* factor within H1 is a separate
gap. `Pipeline` has no Ray execution path, so a `distributed=True`
condition would run identical single-node code and report a distribution
effect of approximately zero — a null result that nothing distributed
produced. `Pipeline` therefore raises rather than running those cells, and
`--ablation local` crosses only the factors that are implemented (chunk
filter x warm start). Measuring the distribution factor requires
implementing the Ray path *and* a second machine; until then it stays
PENDING and no speedup-from-distribution figure may be reported.
