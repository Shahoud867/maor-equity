[pytest]
testpaths = tests
pythonpath = src
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
markers =
    gpu: requires a CUDA device
    network: requires network access (model or dataset download)
    slow: takes more than ~30s
